"use client";

export function DashboardHeader() {
  return (
    <header className="sticky top-0 z-40 flex h-16 items-center justify-end bg-[#fffaf5]/90 px-8 backdrop-blur">
      {/* Header content can go here later (breadcrumbs, page title, etc.) */}
    </header>
  );
}
